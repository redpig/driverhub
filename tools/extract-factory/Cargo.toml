[package]
name = "extract-factory"
version = "0.1.0"
edition = "2021"
description = "Extract DTB, kernel modules, and symvers from Pixel factory images"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
flate2 = "1"
goblin = "0.9"
zip = "2"
